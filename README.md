# D(ev) Rabbit FE

## 📚 설스터디 FE

멘토/멘티를 위한 학습 관리 웹앱 프론트엔드 프로젝트입니다.
React + TypeScript + Vite 기반으로 개발되었습니다.

---

## 🛠️ 기술 스택

* **Framework**: React
* **Language**: TypeScript
* **Bundler**: Vite
* **Routing**: React Router DOM
* **Styling**: Tailwind CSS
* **Icons**: react-icons
* **State / Utils**: clsx (cn 유틸)

---

## ✅ 사전 준비

아래 환경이 필요합니다.

* **Node.js**: `v18 이상` 권장

  ```bash
  node -v
  ```
* **패키지 매니저**: npm

---

## 📦 설치 방법

### 1️⃣ 레포지토리 클론

```bash
git clone <레포지토리 주소>
```

### 2️⃣ 의존성 설치

```bash
npm install
```

> `package.json`에 정의된 모든 라이브러리가 설치됩니다.

---

## ▶️ 실행 방법

### 개발 서버 실행

```bash
npm run dev
```

실행 후 브라우저에서 아래 주소로 접속합니다.

```
http://localhost:5173
```

---

## 📁 프로젝트 구조 (요약)

```txt
src/
 ├─ app/            # AppShell, 전역 레이아웃
 ├─ pages/          # 페이지 단위 (mentor / mentee)
 ├─ widgets/        # Header, Sidebar, Nav 등 UI 블록
 ├─ shared/         # 공통 유틸, UI, 훅
 ├─ router/         # 라우터 설정
 └─ main.tsx
```

---

## 👥 역할별 화면

* **멘토**: PC 웹 중심 UI (Sidebar + Header 고정)
* **멘티**: 모바일 웹앱 UI (Top Header + Bottom Nav 고정)

> 멘토 화면에는 AppShell(모바일 웹앱 스타일)이 적용되지 않습니다.

---

## ⚠️ 문제 발생 시 체크리스트

### 1️⃣ 의존성 문제일 경우

```bash
rm -rf node_modules package-lock.json
npm install
```

### 2️⃣ Node 버전 확인

```bash
node -v
```


## ✨ 기타

* 스타일은 **Tailwind CSS** 기준
* 아이콘은 **react-icons** 사용
* 공통 클래스 병합은 `cn()` 유틸 사용
